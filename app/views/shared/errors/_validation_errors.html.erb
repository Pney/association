<% if object.errors.any? %>
  <div class="error_explanation">
    <h2><%= pluralize(object.errors.count, "error") %> itens foram proibidos de serem salvos:</h2>

    <ul>
      <% object.errors.each do |msg| %>
        <li><%= msg.full_messages %></li>
      <% end %>
    </ul>
  </div>
<% end %>